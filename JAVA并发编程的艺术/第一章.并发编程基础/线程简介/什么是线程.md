1. 现代OS 在执行一个程序时，会为其创建一个进程，列如，当创建一个JAVA程序，OS就会为其创建一个JAVA进程，现代OS调度的最小单元是线程，也叫**轻量级进程**.在同一个进程里可以创建多个线程，这些线程有自己的计数器，堆栈和局部变量的属性，并且能够访问共享的内存变量。处理器在这些线程上高速切换，使其满足在短时间内一同执行
2. 哪怕是在使用main函数中 也调用了四个线程，如**分发和管理JVM的线程，调用对象finallize方法的线程，清除Reference线程以及main线程和用户程序入口**;
