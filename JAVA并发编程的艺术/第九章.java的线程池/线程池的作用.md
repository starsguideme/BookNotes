线程池提供以下三个好处
- **降低资源损耗**:通过重复使用已创建的线程来降低线程创建和销毁造成的损耗
- **提高响应速度**:当任务到达时，任务可以不需要等到线程创建就能立即创建执行
- **提高线程的可管理性**：由于线程的特性导致不可以被无限制的创建，因为这不仅会消耗系统资源的同时还会降低系统的稳定性.为了解决这个麻烦就诞生了线程池，使用线程池可以有效的统一分配，进行调优和监控线程。以下是线程池的核心信息
- [[线程池的实现原理]]: