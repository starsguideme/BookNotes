减少上下文切换的方法一共有无锁并发编程，CAS算法，使用最少线程与使用协程来减少上下文的开销
无锁并发编程:多线程竞争锁时，会引起上下文的切换，所以在处理多线程时，可以用一些办法来避免使用锁
CAS算法:Java的Atomic包使用CAS算法来更新数据，而不需要加锁
使用最少线程:避免创建不需要的线程，比如任务很少，但创建了多线程来处理 这会让大量线程处于等待状态
使用协程:在单线程使用多任务调度，且维持在多任务之间的切换
具体方法参考[[减少上下文切换实战]].