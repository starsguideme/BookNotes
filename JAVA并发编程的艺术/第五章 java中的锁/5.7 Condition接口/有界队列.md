

**有界阻塞队列**是一种容量受限的线程安全队列，它在普通队列的基础上，通过**阻塞控制**来协调生产者和消费者的操作节奏。

其核心机制如下：

1. **有界容量**：队列在创建时设定固定的最大容量，元素个数不能超过该上限。
2. **非空插入**：只要队列未满，插入操作（如`put`）会正常添加新节点，与普通队列无异。
3. **空取阻塞**：当队列为空时，获取操作（如`take`）会阻塞调用线程，直到队列中有新元素可用时自动唤醒。
4. **满插阻塞**：当队列已满时，插入操作会阻塞调用线程，直到队列中有元素被取走腾出空位时自动唤醒。