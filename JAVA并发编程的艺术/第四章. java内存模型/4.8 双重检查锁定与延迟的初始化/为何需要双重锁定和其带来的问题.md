1. 由于某些时候需要降低启动线程的开销与降低类的初始化以此需要某些方法来解决这个问题，曾在历史上使用了双重的检查并锁定线程的延迟初始化.这种方法虽然可以解决延迟初始化的功能，但也带来了一些问题，如遇到了instance引用对象还没完成初始化时，其有可能是为非空的线程，此时双重检查锁定则根本无法执行(因为其本质是一个思想，而不是一个具体的实现工具)
2.双重检查锁定也带来了很多问题，详细参考[[DCL问题与解决方案]]