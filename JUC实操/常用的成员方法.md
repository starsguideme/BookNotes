有 `setPriority(),setName,setDaemon,start,getName()，sleep(),currentThread()方法，yield(),join()`等
其中`setPriority()`这个方法并不可靠 并不能完全解决线程被指定顺序完成
`setName()` 指的是 设置线程的名字，getName则是得到线程的名字，setDaemon()则是将线程设置为守护线程,被守护线程守护的线程一旦结束，则守护线程会根据OS的通知后在执行几次之后就会结束，yield()的作用是让线程尽可能保持均匀，但没有强制性
