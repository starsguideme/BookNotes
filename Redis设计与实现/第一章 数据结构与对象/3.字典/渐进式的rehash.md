由于数据并不是慢慢的到来，而是由于高并发下直接涌入数据库，所以 Redis需要一个机制用来解决这个问题，他采用的方式是分批存储数据
分批存储数据的好处：
1. 不会因为数据的巨量导致Redis的性能不足以处理如此庞大的数据量后导致服务器的崩溃
2. 在数据规模不大的情况下，这个机制会很迅速的存放数据







Rehash的步骤
同时保存未分配槽位和已分配槽位
创建索引，将已分配槽位上的数据通过索引的方式存放到未分配槽位中，然后进行需求来CRUD，进行这些操作后会让索引自增，当所有数据均已经处理完之后，会让这个索引变成-1 然后返回，告诉系统已经完成数据间的存放
