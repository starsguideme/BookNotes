首先跳跃表的节点分别保存了头节点，尾节点，最大层数，长度，层，后退，分值，成员对象
其中 又可以分类zkiplist 和zkiplistNode 两个域
其中zkiplist 存储着每层之间的关系和层与层之间的关系，每层之间又像链表那样保持着关系
而zkiplistNode 则保存着各类节点，分值 成员对象和后退操作
zkiplist和zkiplistNode共同完成了跳表中点与点的计算，遍历和查询

