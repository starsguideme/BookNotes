除了RDB持久化之外，Redis还提供了AOF（Append Only File）持久化功能。与RDB通过保存数据库键值对快照来记录状态不同，AOF通过记录服务器执行过的写命令来保存数据库状态。

**2. 如何用AOF恢复数据**

服务器启动时，会通过载入并重新执行AOF文件中保存的写命令，来还原服务器关闭之前的数据库状态。

**3. AOF的实现方式**

Redis AOF持久化的本质，是将写命令以Redis协议文本的形式，追加保存到AOF缓冲区的末尾。
**4. AOF文件的载入与数据还原**
详细参考下[[Redis读取AOF文件并还原数据库状态的详细步骤]].
