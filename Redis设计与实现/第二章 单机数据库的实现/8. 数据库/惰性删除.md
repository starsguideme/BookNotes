1. **优点**：对 CPU 最友好。程序只会在访问某个键的时候，才会检查它是否过期。过期删除操作只在非做不可的时候执行，而且只针对当前被访问的这个键，不会浪费 CPU 时间去扫描其他键。
    
2. **缺点**：与[[定时删除]]相反，它对内存很不友好。如果一个键过期后再也没有被访问，它就会一直留在内存里，造成内存浪费。如果大量过期键长期无人访问，甚至可能导致内存泄漏。
    
3. **和其他策略的关系**：正因为这个缺点，Redis 没有单独使用惰性删除，而是配合[[定期删除]]策略一起使用。惰性删除负责“访问时顺手清理”，定期删除负责“主动抽查清理”，两者互补。