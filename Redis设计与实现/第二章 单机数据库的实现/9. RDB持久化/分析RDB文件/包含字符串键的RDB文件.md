1. 当一个数据库被保存到RDB文件时，这个数据库将由以下三部分组成:
   1.1 一个一字节长的特殊值SELECTDB
   1.2 一个长度可能为一字节，两字节，五字节的数据库号码
   1.3 一个或以上数量的键值对
2. 没有过期时间的键值对类型，键，值三部分组成，其中类型的长度为1字节，键和值都是字符串对象，并且字符串在没被压缩前，都是以字符串长度为前缀，后跟字符串内容本身的方式进行保存
3. 在RDB文件中 第一个\0 这个值一般都是RDB文件的文件值，第二个整数是第二个字符串的值，以此类推