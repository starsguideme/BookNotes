‌**Redis 的集合键（Set）* 是 Redis 提供的五种基本数据类型之一，用于存储‌**无序且不重复的字符串元素集合**‌。

---

### 核心特性

- ‌**无序性**‌：元素存储顺序不按插入顺序，也不可按索引访问。
- ‌**唯一性**‌：自动去重，重复元素会被忽略。
- ‌**高效操作**‌：添加、删除、查找元素的时间复杂度均为 ‌**O(1)**‌，底层通常使用哈希表实现。
- ‌**支持集合运算**‌：可进行并集（`SUNION`）、交集（`SINTER`）、差集（`SDIFF`）等操作。