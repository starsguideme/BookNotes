简历上可写: 
1. 发现定时任务恢复超时订单库存时存在幂等性问题——同一取消订单可能被多次扫到，导致库存重复恢复。

2. 一开始考虑用 Redis 记录已处理订单，但推演后发现 Redis 宕机会导致标记丢失。最终选择数据库字段防重方案：在订单表增加 `stock_restored` 标记，配合 `WHERE stock_restored = 0` 条件与 Spring 事务，保证操作的原子性和幂等性。

3. 在改造过程中，发现同样的并发控制思路可用于库存扣减，顺带引入乐观锁解决超卖问题。
