我曾在[[内核线程的实现]]中提及过关于内核线程是一个什么东西，其本质是一个轻量级线程在由操作系统自己调度下的一个线程模型，它只能由操作系统内核态支持后才可以执行，因此它是1对1的模型，也正因如此，所以内核线程的执行对内存资源上来的浪费，开销与执行上下文切换都是一件很麻烦的事的同时，由于系统资源有限，所以能容纳的内核线程也比较少
正因如此再加上由于经典的虚拟机很早之前就被淘汰了 所以就带了一个问题，怎么解决内核线程带来的问题呢，这就是协程的作用，详见于[[协程的复苏]].