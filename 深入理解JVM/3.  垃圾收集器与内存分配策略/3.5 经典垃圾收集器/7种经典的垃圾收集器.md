在HotSpot的收集器中一共可以有7种：
- Serial收集器： 单线程，客户端小内存场景用，服务端不用"
- ParNew收集器：Serial 多线程版，以前配合 CMS 用，现在没单独存在的意义了"
- Parallel Scavenge收集器: JDK 8 默认，吞吐量优先，适合后台批处理，不适合交互式应用
- Serial old收集器：Serial 的老年代版本"
- Parallel Old收集器
- [[CMS收集器]]：
- [[Garbage First收集器]]：