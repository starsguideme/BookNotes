·
### 一、核心作用

`jhat` 一般与 `jmap` 搭配使用，主要是分析 `jmap` 生成后的堆转储快照。`jhat` 内置了一个 **HTTP/Web 服务器**，生成堆转储快照的分析结果后可以在浏览器中查看。

### 二、为什么 jhat 用得比较少？

在实际中，`jhat` 用得比较少，这是因为它有两个比较致命的缺点：

| 缺点 | 说明 |
|------|------|
| ==**不适合在服务器上直接分析**== | 一般不会在部署应用程序的服务器上直接分析堆转储快照，会尽可能把堆转储快照文件复制到其他大型服务器上进行分析，因为分析工作是比较耗时且会使用较多资源的。因此一般**不推荐在命令行中使用** |
| ==**性能较弱**== | `jhat` 的分析性能较差，对于大堆快照的分析非常缓慢 |

### 三、jhat 的分析结果

`jhat` 的分析结果会默认以**包为单位**进行分组显示。分析 OOP 主要会使用到其中的 **Heap Histogram**，该功能与 `jmap` 的 `-histo` 功能类似，也和 **OQL** 标签的功能类似：

| 功能 | 说明 |
|------|------|
| ==**Heap Histogram**== | 显示堆中**总容量最大的对象**的直方图 |
| ==**OQL（Object Query Language）**== | **标准对象查询语言**，用于查询堆中的对象 |

### 四、面试要点

| **问题**                         | 回答                                          |
| -------------------------- | ------------------------------------------- |
| **`jhat` 工具的作用**               | 分析 `jmap` 生成的**堆转储快照**，内置 HTTP 服务器，可在浏览器中查看 |
| **为什么 `jhat` 用得少**             | ==**分析耗时且占资源**==，不适合在服务器上直接运行；==**性能较弱**==  |
| **`jhat` 的 Heap Histogram 功能** | 显示堆中**总容量最大的对象**的直方图，与 `jmap -histo` 类似     |
| **OQL 是什么**                    | ==**对象查询语言**==，用于查询堆中的对象                    |
