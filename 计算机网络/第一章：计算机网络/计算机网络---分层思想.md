1. 后端内容通过HTTP和路由器的形式访问服务器途中会经过各个节点，服务器最后会通过原路返回数据.
2. 数据传输是如何运行的 首先得考应用层用HTTP的格式返回给运输层，然后运输层给这个报文段加上TCP的首段然后给IP，网络层再为此加上IP的字段，然后网络层在将这个数据给数据链路层 数据链路层在为此加上了一个首段一个尾段，分别是LEN和ETH并将其给予物理层 然后物理层会将其变成一段01文本后会将其给传输媒体后进行转发，每个转发的内容时候会截取各首段的内容进行读取后将剩余的数据转发至下一个节点