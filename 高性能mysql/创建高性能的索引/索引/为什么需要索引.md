1. 为什么要用索引：当数据量达到一定量级后，全表扫描会变得非常缓慢。加上合理的索引，能极大加快查询速度。
    
2. 索引是什么：索引的本质是一个帮助MySQL高效获取数据的数据结构，最常用的是B+树。但索引不是越多越好，设计不合理或冗余的索引会拖慢写入性能、浪费存储空间。
    
3. 索引虽然能加速查询，但每次插入，修改和删除的操作都需要同时维护索引结构。所以建索引的原则是“够用就好”，不要为不确定的目标字段创建不必要的索引
4. 索引的优点:[[使用索引的优点]].