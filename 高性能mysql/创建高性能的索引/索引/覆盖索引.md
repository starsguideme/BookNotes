1. 可以只访问索引，而无须访问数据行的方式叫做覆盖索引
2. 案例如下:SELECT `user_id`, status, `create_time` 
FROM orders 
WHERE `user_id` = 1;
3. 其中要想触发这个覆盖索引 需要满足其中三个字段是已经创建了联合索引，并且不能访问额外的字段，如果在联合索引的基础上多访问了额外字段，则覆盖索引会失效，他会回表，进行全表扫描访问数据